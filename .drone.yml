kind: pipeline
# type: kubernetes
name: default
volumes:
- name: docker_sock
  host:
    path: /var/run/docker.sock
steps:
- name: test
  image: docker
  volumes:
    - name: docker_sock
      path: /var/run/docker.sock
  commands:
  - docker version
  - echo ${HELLO}
